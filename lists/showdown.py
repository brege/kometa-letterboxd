"""Placeholder for Letterboxd Showdown integrations."""


def generate_showdown_collections(all_lists, showdown_config):
    """Build collections from showdown settings.

    The implementation is intentionally left empty until showdown rules are
    defined. The function signature exists so callers can plug in the module
    without changing call sites later.
    """

    _ = (all_lists, showdown_config)  # quiet linters about unused params
    return {}
